# for basic scRNAseq analyses that don't require gpu:
name: hlca2

dependencies:
 - python=3.12 #before was 3.7
 - pip
 - pip:
   - jupyterlab
   - scanpy==1.10.4 #==1.9
   - umap-learn>=0.5
   - pynndescent
   #- nb-black # for pretty code formatting
   - leidenalg
   - adjustText
   - matplotlib_venn
   - seaborn==0.13
   - polars
   - pyarrow
   - itables
